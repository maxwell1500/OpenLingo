<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Media3 ExoPlayer declares ACCESS_NETWORK_STATE for streaming; the app
         only plays bundled local assets, so strip the merged permission. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" tools:node="remove" />
    <application
        android:name=".DuoApplication"
        android:label="OpenLingo"
        android:theme="@style/Theme.Duo"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:screenOrientation="unspecified"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Midnight streak-reset + heart-refill alarm (Section: Streaks) -->
        <receiver
            android:name=".alarm.DailyResetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.duo.app.action.DAILY_RESET" />
            </intent-filter>
        </receiver>

        <!-- Reschedule the midnight alarm after reboot -->
        <receiver
            android:name=".alarm.BootRescheduleReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!-- Daily 7 PM streak reminder alarm -->
        <receiver
            android:name=".alarm.StreakReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.duo.app.action.STREAK_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- OpenLingo Home Screen Widget -->
        <receiver
            android:name=".widget.OpenLingoWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_openlingo_info" />
        </receiver>
    </application>

</manifest>
